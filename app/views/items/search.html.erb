<% if @items.empty? %>
    <h1>Result not found!</h1>
<% else %>
    <div class="row">

      <div class="col-lg-12">
        <h1 class="page-header">Search Results</h1>
      </div>
      <% @items.each do |item| %>
          <%= render 'home/top_newest_item', item: item %>
      <% end %>
    </div>
<% end %>